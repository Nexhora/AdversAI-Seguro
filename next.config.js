// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true, // Recommended for new apps
  async redirects() {
    return [];
  },
};

module.exports = nextConfig;
